<!-- Image and text -->
<nav class="navbar">
  <div class="navbar-brand px-3">
    SUPER TORTAS TAMPICO
  </div>
  <div class="d-flex flex-row">
    <% if user_signed_in? %>
      <div class="px-3">
        <%= link_to "Home", "#", class: "nav-link" %>
      </div>
      <div class="px-3">
        <%= link_to "Messages", "#", class: "nav-link" %>
      </div>
      <div class="px-3">
        <%= button_to 'Logout', destroy_user_session_path, method: :delete, form: {turbolink: false}, class: "log-out-button" %>
      </div>
    <% else %>
      <div class="px-3">
        <%= link_to "Login", new_user_session_path, class: "nav-link" %>
      </div>
    <% end %>
  </div>
</nav>
